{
  "name": "graph-algorithms",
  "private": "true",
  "scripts": {
    "test": "jest",
    "lint": "eslint . --ext ts",
    "lint:fix": "eslint . --ext ts --fix",
    "format": "prettier . --write --ignore-path node_modules/@cv9t/prettier-config/.prettierignore",
    "prep": "pnpm format && pnpm lint:fix && git add ."
  },
  "devDependencies": {
    "@cv9t/eslint-config": "^0.0.2",
    "@cv9t/prettier-config": "^0.0.1",
    "@jest/globals": "^29.7.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
